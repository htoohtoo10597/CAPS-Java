spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/caps?useSSL=false&serverTimezone=Asia/Singapore&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=gdipsa54
spring.datasource.password=gdipsa54

#to get springboot to read the embedded .sql files in src/main/resources
spring.sql.init.mode=always

#to delay the initialization of embedded .sql files so that springboot can create the schema & tables first
spring.jpa.defer-datasource-initialization=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
